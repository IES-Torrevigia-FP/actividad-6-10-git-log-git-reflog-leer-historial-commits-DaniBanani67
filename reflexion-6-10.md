# Reflexión

git log sirve para ver el historial normal de commits del proyecto

git reflog muestra los movimientos de HEAD y permite ver estados anteriores incluso si los commits ya no aparecen en el log

git reflog puede salvarte si haces un reset --hard o borras una rama por error

Aun asi hay que tener cuidado con reset --hard porque puede eliminar cambios del working directory